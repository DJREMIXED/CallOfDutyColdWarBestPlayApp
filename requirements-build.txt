# Fan Cave Studio PRO — Clip Toolkit
# Build-time only: the toolchain that packages FanCaveStudio.app.
#
# This one matters even more than requirements.txt. PyInstaller runs on your
# machine and writes the app your users download, so a compromised release here
# would end up inside the shipped binary.
#
# Not needed to run the app from source — only to build the .app bundle.
#
# Last reviewed: 2026-08-30

pyinstaller==6.22.2

# Pulled in by PyInstaller. Pinned so the whole build tree is fixed.
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
macholib==1.16.4
packaging==26.3
setuptools==84.0.0
